<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Requested at the point of use, never all at once on first launch. An OTP
      forwarder that asks for everything up front is indistinguishable from
      spyware, and the user has no way to tell the difference.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".EasyOtpApp"
        android:label="@string/app_name"
        android:theme="@style/Theme.EasyOTP"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:supportsRtl="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Declared in the manifest, not registered at runtime: SMS_RECEIVED is
          exempt from the implicit-broadcast ban, so this fires even when the app
          process is dead. That property is the whole reason the capture path is
          native rather than in a cross-platform runtime (DECISIONS D1).
        -->
        <receiver
            android:name=".capture.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!--
          specialUse, not dataSync: Android 15 caps dataSync foreground services
          at six hours per twenty-four, which would kill the forwarder daily and
          silently on a phone nobody is watching (DECISIONS D7).
        -->
        <service
            android:name=".service.ForwarderService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Forwards SMS from SIMs held by the device owner to their own Telegram bot, which requires a persistent process because messages arrive at any time." />
        </service>

        <!--
          The device is unattended in another country. Without this, a power cut
          or an OS update stops forwarding until someone physically opens the app.
        -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
